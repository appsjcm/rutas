(function(){
'use strict';
const $=id=>document.getElementById(id),C=window.RutasNav,H=window.RutasHudCore;
// Las capas que flotan sobre el mapa se apilan con estas tres medidas: cartel de maniobra,
// tarjeta de la calle y aviso de via. Cada una empuja a la siguiente, y la barra de vistas
// va al final.
//
// Se miden con ResizeObserver, pero no SOLO con el: su entrega va atada al ciclo de
// pintado, y en una pestaña que el navegador no esta dibujando no llega. Entonces las
// variables se quedan sin poner, el CSS cae a los valores por defecto y la tarjeta se
// planta encima de la barra -medido en el sitio publicado: 242x23 px de solape-. Que la
// maquetacion de la pantalla de conduccion dependa de que un observador llegue a tiempo es
// demasiado fragil para algo que se mira conduciendo, asi que se remide tambien en cada
// actualizacion de posicion, que es cuando de verdad importa que este bien.
function mideCapas(){
 const stage=$('nav-stage');
 if(!stage)return;
 const pon=(nombre,el,margen)=>{
  if(!el)return;
  const h=el.hidden?0:el.getBoundingClientRect().height;
  stage.style.setProperty(nombre,h?(h+margen)+'px':'0px');
 };
 // Lo que necesita lo de abajo no es cuanto mide el cartel, sino donde acaba: en
 // simulacion el cartel arranca 24 px mas abajo para no quedar bajo la franja roja, y con
 // la altura a secas la tarjeta de la calle se le metia debajo. Se mide el borde inferior
 // respecto al escenario, que es el origen de las posiciones de estas capas.
 const cartel=$('drive-banner');
 if(cartel&&!cartel.hidden){
  const c=cartel.getBoundingClientRect(),base=stage.getBoundingClientRect().top;
  const abajo=c.bottom-base;
  if(abajo>0)stage.style.setProperty('--drive-banner-height',abajo+'px');
 }
 pon('--drive-road-height',$('drive-road-info'),8);
 pon('--drive-alert-height',$('nav-road-alert'),8);
}
for(const el of [$('drive-banner'),$('drive-road-info'),$('nav-road-alert')])
 if(el)new ResizeObserver(mideCapas).observe(el);
// La tarjeta de la calle empuja hacia abajo la barra de vistas: en un movil no caben en la
// misma fila y la tarjeta tapaba 2D, Satelite y 3D. Cuando no hay tarjeta, cero.

// Y el aviso de via empuja la barra un escalon mas: primero se lee la advertencia.

let checkingStart=false,access=null;
let resumeKey=null,lastSaved=0,resumePoint=null,lastSpeedVoice=0,lastSpeedLimit=null;
let voiceStages=new Map(),liveSpeed=0,currentInstruction="Inicia la navegación para recibir indicaciones.",lastReceived=0,gpsTimer=null,guideState="preview";
let arrowLayer,completed,turns=[],baseTurns=[],stopList=[],announced=-1,lastPosition=null,headingUp=false,lastBearing=0,roadVoiceStages=new Set(),offRouteCount=0;
let map,streetTiles,satelliteTiles,satelliteRoads,satelliteLabels,satellite=false,layer,preview,position,accuracy,remaining,route,progress=0,explore=0,watch=null,lock=null,session=0,playing=null,follow=true,lastFix=null,lastVoice=0,lastAlert='',routeRef;
const metres=m=>m<1000?Math.round(m)+' m':(m/1000).toFixed(2).replace('.',',')+' km';
function message(text,warn=false){$('drive-status').textContent=text;$('drive-status').classList.toggle('warn',warn);$('nav-message').textContent=text;$('nav-message').classList.toggle('warn',warn);}
function speak(text,force=false){if(!$('nav-voice').checked||!window.speechSynthesis)return;if(!force&&text===lastAlert&&Date.now()-lastVoice<30000)return;lastVoice=Date.now();lastAlert=text;window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang='es-ES';window.speechSynthesis.speak(u);}
function ll(p){return [p.lat,p.lon];}
function init(){if(map)return true;if(!window.L){message('No se pudo cargar el mapa. Recarga la página.',true);return false;}map=L.map('nav-map').setView([40.4,-3.7],6);streetTiles=L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a>'}).on('tileerror',()=>message('No se pudo cargar parte del mapa. Comprueba tu conexión.',true)).addTo(map);satelliteTiles=L.tileLayer('https://services.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}',{maxZoom:19,attribution:'&copy; Esri, Maxar, Earthstar Geographics, GIS User Community'}).on('tileerror',()=>message('No se pudo cargar parte del mapa satélite. Comprueba tu conexión.',true));satelliteRoads=L.tileLayer('https://services.arcgisonline.com/ArcGIS/rest/services/Reference/World_Transportation/MapServer/tile/{z}/{y}/{x}',{maxZoom:19,opacity:.82,attribution:'Carreteras y rótulos &copy; Esri'});satelliteLabels=L.tileLayer('https://services.arcgisonline.com/ArcGIS/rest/services/Reference/World_Boundaries_and_Places/MapServer/tile/{z}/{y}/{x}',{maxZoom:19,opacity:.95});L.control.scale({imperial:false}).addTo(map);map.on('dragstart',()=>{follow=false;$('drive-center').classList.add('needs-center');$('drive-center').textContent='Volver a seguir';controlLabel('drive-center','Volver a seguir mi posición');});map.on('zoomend',()=>{if(route&&layer)drawArrows();});return true;}
function setSatellite(value){if(!init())return false;satellite=Boolean(value);$('nav-stage').classList.toggle('satellite-view',satellite);if(satellite){if(map.hasLayer(streetTiles))map.removeLayer(streetTiles);for(const tile of [satelliteTiles,satelliteRoads,satelliteLabels])if(!map.hasLayer(tile))tile.addTo(map);satelliteTiles.bringToBack();}else{for(const tile of [satelliteTiles,satelliteRoads,satelliteLabels])if(map.hasLayer(tile))map.removeLayer(tile);if(!map.hasLayer(streetTiles))streetTiles.addTo(map);streetTiles.bringToBack();}map.invalidateSize();return satellite;}
function spin(){const stage=$('nav-stage'),mp=$('nav-map'),deg=headingUp?-lastBearing:0;
 stage.style.setProperty('--bearing',deg.toFixed(1)+'deg');
 mp.style.transform=headingUp?'rotate('+deg.toFixed(1)+'deg)':'';}
function controlLabel(id,label){const button=$(id);button.setAttribute('aria-label',label);button.title=label;}
function clock(seconds){const t=new Date(Date.now()+seconds*1000);return t.toLocaleTimeString('es-ES',{hour:'2-digit',minute:'2-digit'});}
function spell(seconds){const m=Math.round(seconds/60);if(m<60)return m+' min';const h=Math.floor(m/60);return h+' h '+String(m%60).padStart(2,'0');}
// Pintar la cabecera es una sola cosa y pasa por un solo sitio; lo que dice, lo decide H.
// Lo que se ha quedado sin pasar, si el registro de tramos esta cargado. Al final no se
// puede decir «completado» si no lo es: el cartel lo afirmaba encima del aviso contrario.
function falta(){try{return (window.RutasCoverage&&window.RutasCoverage.summary())||'';}catch(e){return '';}}
function faltaVoz(){try{return (window.RutasCoverage&&window.RutasCoverage.voice&&window.RutasCoverage.voice())||'';}catch(e){return '';}}
function paintBanner(b){
 $('drive-banner').dataset.phase=b.tone;
 $('drive-turn-arrow').parentElement.dataset.exit=b.exit||'';
 $('drive-turn-phase').textContent=b.eyebrow;
 $('drive-turn-arrow').textContent=b.arrow;
 $('drive-turn-dist').textContent=b.lead;
 $('drive-turn-action').textContent=b.action;
 $('drive-turn-street').textContent=b.street;
 $('drive-upcoming').textContent=b.after;
}
function hud(d,g,end){
 paintBanner(H.banner({end,missing:end?falta():'',stage:g.stage,gap:g.gap,turn:g.turn,next:g.next,access}));
 // remaining time: the recorded round itself is the best estimate; live speed is the fallback
 let left=access?access.duration*Math.max(0,1-d/route.total):C.remainingSeconds(route,d);
 if(left===null){const v=watch!==null&&liveSpeed>1?liveSpeed:null;left=v?(route.total-d)/v:null;}
 $('drive-eta-time').textContent=left===null?'—':spell(left);
 $('drive-eta-clock').textContent=left===null?'—':clock(left);
 const st=C.stopProgress(stopList,d);
 $('drive-next').textContent=st.total?(st.next?'Parada '+(st.done+1)+' de '+st.total+' a '+metres(st.gap):'Paradas completadas: '+st.total+' de '+st.total):'';
}
function metrics(d){$('nav-done').textContent=metres(d);const left=metres(Math.max(0,route.total-d));$('nav-left').textContent=left;$('drive-left').textContent=left;$('drive-progress').value=route.total?100*d/route.total:0;const s=C.stopProgress(stopList,d);$('nav-stops').textContent=s.total?s.done+' de '+s.total:'—';$('nav-stops').title=s.total?(s.next?'Siguiente parada a '+metres(s.gap):'Todas las paradas hechas'):'Este GPX no trae horas, o no registra ninguna parada de 3 minutos o más';}
function showPreview(d,pan=false){if(!route)return;explore=Math.max(0,Math.min(route.total,d));const p=C.at(route,explore);preview.setLatLng(ll(p));$('nav-progress').value=route.total?Math.round(explore/route.total*1000):0;$('nav-preview-distance').textContent=metres(explore);paint(explore);if(watch===null)metrics(explore);if(pan){map.panTo(ll(p));window.dispatchEvent(new Event('rutas:preview-pan'));}}
function pause(){if(playing)clearInterval(playing);playing=null;$('nav-play').textContent='Reproducir';}
function controls(active){
 setDriving(active);
 const banner=$('drive-banner');banner.hidden=!active;banner.setAttribute('aria-hidden',String(!active));
 $('drive-speedo').hidden=!active;$('drive-exit').hidden=!active;$('nav-focus-stop').hidden=!active;
 $('nav-start').disabled=active;$('nav-stop').disabled=!active;
 ['nav-progress','nav-back','nav-next','nav-play','nav-reset','nav-resume','nav-locate-pass'].forEach(id=>$(id).disabled=active);
 $('nav-mode').textContent=access?(access.recovery?'Regreso al GPX':'Acceso al GPX'):active?'GPS activo':'Vista previa';
 const exit=$('drive-exit');exit.textContent=access?(access.recovery?'Cancelar regreso':'Cancelar acceso'):'Finalizar';exit.setAttribute('aria-label',access?(access.recovery?'Cancelar el regreso al recorrido':'Cancelar el acceso al inicio'):'Finalizar la navegación');exit.title=exit.getAttribute('aria-label');
 const voiceOn=$('nav-voice').checked;controlLabel('drive-voice',voiceOn?'Desactivar indicaciones por voz':'Activar indicaciones por voz');
 controlLabel('drive-center','Centrar mi posición');
 if(active){lastSpeedVoice=0;lastSpeedLimit=null;}
}
function stop(quiet=false,preserveAccess=false){checkingStart=false;offRouteCount=0;window.dispatchEvent(new Event('rutas:on-route'));if(watch!==null)saveProgress(true);session++;if(watch!==null)navigator.geolocation.clearWatch(watch);watch=null;lastFix=null;lastPosition=null;if(gpsTimer)clearInterval(gpsTimer);gpsTimer=null;guideState="preview";if(lock){lock.release().catch(()=>{});lock=null;}controls(false);if(window.speechSynthesis)window.speechSynthesis.cancel();if(!quiet){if(resumeKey)restoreProgress();if(route)showPreview(progress);message('GPS detenido. Puedes explorar la ruta o continuar desde el punto seleccionado.');currentInstruction='Navegación detenida.';$('nav-live-summary').textContent=currentInstruction;}if(access&&!preserveAccess){const saved=access;access=null;routeRef=null;sync();showPreview(saved.targetD,true);message('Acceso detenido. Tu GPX está preparado en el punto elegido.');window.dispatchEvent(new Event('rutas:access-ended'));}}
function sync(){if(!init())return;const original=window.Roadbook.getRoute(),data=access?{pts:access.pts,name:access.recovery?'Regreso al recorrido':'Acceso al punto de inicio',sample:false,tracks:[{name:'Acceso por calles',pts:access.pts}],selected:0}:original;if(data.pts===routeRef){map.invalidateSize();return;}stop(true,true);pause();routeRef=data.pts;route=C.prepare(data.pts);resumeKey=access?null:'rutas-progress-v1:'+C.fingerprint(route);lastSaved=0;restoreProgress();progress=0;explore=0;if(layer)map.removeLayer(layer);if(position){map.removeLayer(position);position=null;}if(accuracy){map.removeLayer(accuracy);accuracy=null;}layer=L.featureGroup().addTo(map);const coords=data.pts.map(ll);L.polyline(coords,{color:'#ffffff',weight:9,opacity:.9}).addTo(layer);L.polyline(coords,{color:'#6e8090',weight:5,opacity:.85}).addTo(layer);completed=L.polyline([],{color:'#248660',weight:5,opacity:.9}).addTo(layer);remaining=L.polyline([],{color:'#176dcc',weight:6}).addTo(layer);for(const [p,label,cl] of [[coords[0],'A',''],[coords.at(-1),'B','end']])L.marker(p,{icon:L.divIcon({className:'nav-pin '+cl,html:label,iconSize:[28,28]})}).addTo(layer);baseTurns=access?access.turns:C.turns(route);turns=baseTurns;stopList=access?[]:C.stops(route);announced=-1;voiceStages.clear();drawTurns();drawArrows();preview=L.circleMarker(coords[0],{radius:8,color:'#fff',weight:3,fillColor:'#176dcc',fillOpacity:1}).addTo(layer);map.fitBounds(layer.getBounds(),{padding:[25,25],maxZoom:17});$('nav-name').textContent=(data.sample?'Ejemplo ficticio · carga tu GPX para navegar':'Archivo: '+data.name)+' · '+metres(route.total)+' · '+data.pts.length.toLocaleString('es-ES')+' puntos';$('nav-track').replaceChildren(...data.tracks.map((t,i)=>{const o=document.createElement('option');o.value=i;o.textContent=t.name;o.selected=i===data.selected;return o;}));$('nav-start').disabled=data.sample||route.total<1;message(data.sample?'Vista previa de ejemplo. Carga un GPX real para iniciar el GPS.':'Ruta preparada. Explora las calles o inicia el GPS.');$('nav-speed').textContent='—';$('nav-accuracy').textContent='—';showPreview(0);saveLocal(data);window.dispatchEvent(new Event("rutas:check-route"));}
function update(pos,token){if(token!==session||watch===null)return;lastReceived=Date.now();if(Date.now()-pos.timestamp>30000){guideWarning('Posición GPS antigua. Esperando una posición actual.');return;}const c=pos.coords,p={lat:c.latitude,lon:c.longitude};liveSpeed=Number.isFinite(c.speed)?Math.max(0,c.speed):0;$('nav-accuracy').textContent=Math.round(c.accuracy)+' m';$('nav-speed').textContent=c.speed!==null&&c.speed>=0?Math.round(c.speed*3.6)+' km/h':'—';$('drive-speed').textContent=c.speed!==null&&c.speed>=0?String(Math.round(c.speed*3.6)):'—';if(!position){position=L.marker(ll(p),{zIndexOffset:1000,icon:L.divIcon({className:'nav-vehicle',html:'<span>●</span>',iconSize:[40,40],iconAnchor:[20,20]})}).addTo(map);accuracy=L.circle(ll(p),{radius:c.accuracy,color:'#006dff',weight:1,fillOpacity:.08}).addTo(map);}position.setLatLng(ll(p));accuracy.setLatLng(ll(p)).setRadius(c.accuracy);const bearing=Number.isFinite(c.heading)?c.heading:lastPosition&&C.distance(lastPosition,p)>3?C.heading(lastPosition,p):null;const symbol=position.getElement()?.querySelector('span');if(symbol){symbol.textContent=bearing===null?'●':'';symbol.classList.toggle('heading-known',bearing!==null);symbol.style.transform=bearing===null?'':'rotate('+bearing+'deg)';}if(bearing!==null){lastBearing=bearing;if(headingUp)spin();}if(follow)followCamera(p);window.dispatchEvent(new Event('rutas:gps'));if(c.accuracy>60){guideWarning('Señal GPS imprecisa. Esperando una posición más fiable.');return;}if(checkingStart){checkingStart=false;const gap=C.distance(p,C.at(route,progress));if(gap>100){const selected=progress;stop(true);message('Estás a '+metres(gap)+' en línea recta del punto de inicio. Puedes llegar hasta él antes de seguir el GPX.');window.dispatchEvent(new CustomEvent('rutas:approach',{detail:{distance:gap,progress:selected}}));return;}}const elapsed=lastFix?(pos.timestamp-lastFix)/1000:0;const allowance=Math.min(350,Math.max(250,elapsed*30));const travelled=lastPosition?C.distance(lastPosition,p):0;const expected=progress+Math.min(travelled,allowance);const course=Number.isFinite(c.heading)&&liveSpeed>2?c.heading:lastPosition&&travelled>8?C.heading(lastPosition,p):null;const m=C.match(route,p,progress,allowance,expected,course);lastPosition=p;lastFix=pos.timestamp;if(!m||m.error>Math.max(35,c.accuracy*1.5)){offRouteCount++;if(!access&&offRouteCount>=2){const candidate=C.rejoin(route,p,progress,{minAhead:Math.max(100,liveSpeed*12),course});if(candidate)window.dispatchEvent(new CustomEvent('rutas:off-route',{detail:{origin:p,target:candidate.target,targetD:candidate.d,ahead:candidate.ahead,gap:m?.error||candidate.error}}));}guideWarning(access?'Fuera del acceso previsto. Pulsa Recalcular acceso para obtener otro trayecto.':'Fuera del recorrido. Puedes volver a un punto futuro por calles.');speak('Te has alejado del recorrido. Comprueba el mapa.');return;}if(offRouteCount){offRouteCount=0;window.dispatchEvent(new Event('rutas:on-route'));}guideState="live";progress=Math.max(progress,m.d);showPreview(progress);metrics(progress);saveProgress();announceGuidance();if(route.total-progress<(access?25:15)&&C.distance(p,route.pts.at(-1))<(access?35:25)){if(access){const returning=access.recovery;stop(true);message(returning?'Has regresado al recorrido. La ruta GPX continúa desde este punto.':'Has llegado al acceso del recorrido. Pulsa Iniciar navegación para seguir tu GPX.');speak(returning?'Has regresado al recorrido. Ya puedes continuar con tu ruta.':'Has llegado al inicio del recorrido. Ya puedes continuar con tu ruta.',true);window.dispatchEvent(new Event('rutas:access-arrived'));return;}progress=route.total;showPreview(progress);metrics(progress);stop(true);$('nav-start').disabled=false;const pendiente=falta();currentInstruction='Has llegado al final del recorrido.'+(pendiente?' '+pendiente+'.':'');$('nav-live-summary').textContent=currentInstruction;message(currentInstruction,!!pendiente);speak('Has llegado al final del recorrido.'+(pendiente?' '+faltaVoz():''));return;}message((access?(access.recovery?'Regresando al recorrido · ':'Hacia el inicio · '):'En ruta · ')+'sigue el tramo azul · quedan '+metres(route.total-progress));}
function previewPosition(pos){if(!map||!pos||!pos.coords||watch!==null)return false;const c=pos.coords,p={lat:Number(c.latitude),lon:Number(c.longitude)};if(!Number.isFinite(p.lat)||!Number.isFinite(p.lon))return false;if(!position){position=L.marker(ll(p),{zIndexOffset:1000,icon:L.divIcon({className:'nav-vehicle',html:'<span>●</span>',iconSize:[40,40],iconAnchor:[20,20]})}).addTo(map);accuracy=L.circle(ll(p),{radius:Number(c.accuracy)||0,color:'#006dff',weight:1,fillOpacity:.08}).addTo(map);}position.setLatLng(ll(p));accuracy.setLatLng(ll(p)).setRadius(Number(c.accuracy)||0);$('nav-accuracy').textContent=Number.isFinite(c.accuracy)?Math.round(c.accuracy)+' m':'—';map.setView(ll(p),17);return true;}
function useStreetPath(pts,roadTurns=[]){const data=Roadbook.getRoute();if(access||data.sample||data.pts!==routeRef||!Array.isArray(pts)||pts.length<2)return false;const enMarcha=watch!==null,hecho=progress,antes=route?route.total:0;if(!enMarcha)pause();pts=C.transferTimes(data.pts,pts);route=C.prepare(pts);progress=enMarcha?C.remapProgress(hecho,antes,route.total):0;if(enMarcha&&lastPosition){const enganche=C.match(route,lastPosition,Math.max(0,progress-250),500,progress);if(enganche&&enganche.error<70)progress=enganche.d;}explore=progress;announced=-1;voiceStages.clear();if(layer)map.removeLayer(layer);layer=L.featureGroup().addTo(map);const coords=pts.map(ll);L.polyline(coords,{color:'#ffffff',weight:9,opacity:.92}).addTo(layer);L.polyline(coords,{color:'#526e82',weight:5,opacity:.9}).addTo(layer);completed=L.polyline([],{color:'#248660',weight:5,opacity:.9}).addTo(layer);remaining=L.polyline([],{color:'#176dcc',weight:6}).addTo(layer);for(const [p,label,cl] of [[coords[0],'A',''],[coords.at(-1),'B','end']])L.marker(p,{icon:L.divIcon({className:'nav-pin '+cl,html:label,iconSize:[28,28]})}).addTo(layer);baseTurns=Array.isArray(roadTurns)&&roadTurns.length?roadTurns:C.turns(route,{minSpeed:0});turns=baseTurns;const origen=C.prepare(data.pts);stopList=C.stops(origen).map(s=>({...s,d:C.remapProgress(s.d,origen.total,route.total),start:C.remapProgress(s.start,origen.total,route.total),end:C.remapProgress(s.end,origen.total,route.total)}));drawTurns();drawArrows();preview=L.circleMarker(coords[0],{radius:8,color:'#fff',weight:3,fillColor:'#176dcc',fillOpacity:1}).addTo(layer);if(!enMarcha){map.fitBounds(layer.getBounds(),{padding:[25,25],maxZoom:17});$('nav-progress').value=0;}$('nav-name').textContent='Recorrido por calles · '+metres(route.total)+' · '+pts.length.toLocaleString('es-ES')+' puntos viales · '+baseTurns.length+' maniobras';showPreview(progress);if(enMarcha){paint(progress);metrics(progress);message('Calles reconocidas: la navegación continúa sobre ellas sin perder el avance.');}else message('Recorrido ajustado a calles con indicaciones viales. La simulación y la navegación seguirán este trazado.');window.dispatchEvent(new Event('rutas:navigation-path'));return true;}
async function acquire(token){if(!navigator.wakeLock||document.hidden)return;try{const l=await navigator.wakeLock.request('screen');if(token===session&&watch!==null)lock=l;else await l.release();}catch{}}
$('nav-start').onclick=()=>{if(!route||watch!==null||window.Roadbook.getRoute().sample)return;if(!navigator.geolocation){message('Este navegador no permite acceder al GPS.',true);return;}pause();progress=explore;checkingStart=!access;follow=true;const token=++session;controls(true);guideState='waiting';voiceStages.clear();currentInstruction='Navegación iniciada. Buscando tu posición.';speak(currentInstruction,true);message('Buscando tu posición…');lastReceived=Date.now();gpsTimer=setInterval(()=>{if(watch!==null&&Date.now()-lastReceived>20000){guideWarning('Sin señal GPS reciente. Indicaciones pausadas hasta recuperar la posición.');}},2000);try{watch=navigator.geolocation.watchPosition(p=>update(p,token),e=>{if(token!==session)return;if(C.gpsFatal(e.code)){stop(true);message('Permiso de ubicación denegado. Actívalo en los ajustes del navegador.',true);return;}guideWarning(C.gpsPause(e.code));},{enableHighAccuracy:true,maximumAge:0,timeout:20000});acquire(token);}catch{stop(true);message('No se pudo iniciar el GPS.',true);}};

function guidanceText(g){if(!g.turn)return 'Continúa '+metres(g.gap)+' hasta el final del recorrido.';const action=H.lower(H.imperative(g.turn.label))+(g.turn.toRoad?' en '+g.turn.toRoad:'');const next=g.stage==='now'&&g.next&&g.next.d-g.turn.d<=100?' Después, en '+metres(g.next.d-g.turn.d)+', '+H.lower(H.imperative(g.next.label))+'.':'';return (g.stage==='now'?'Ahora: '+action+'.':'En '+H.distance(g.gap)+', '+action+'.')+next;}
function guideWarning(text){if(guideState!=='paused'&&window.speechSynthesis)window.speechSynthesis.cancel();guideState='paused';paintBanner(H.banner({paused:true,arrow:'!'}));$('drive-next').textContent='';$('drive-eta-time').textContent='—';$('drive-eta-clock').textContent='—';currentInstruction=text;message(text,true);$('nav-turn-icon').textContent='!';$('nav-turn-text').textContent='Indicaciones pausadas';$('nav-turn-distance').textContent=text;$('nav-after').textContent='';$('nav-live-summary').textContent='Esperando una posición válida sobre el recorrido';}
function announceGuidance(){const info=window.RutasChecks?.alertInfo?.(progress);if(info){const stage=info.distance>80?'ahead':'now',key=info.id+':'+stage;if(!roadVoiceStages.has(key)){roadVoiceStages.add(key);currentInstruction=info.voice;speak(info.voice);return;}}const limit=window.RutasChecks?.speedLimitAt?.(progress),kph=liveSpeed*3.6,now=Date.now();if(limit&&kph>=limit+8&&(limit!==lastSpeedLimit||now-lastSpeedVoice>45000)){lastSpeedLimit=limit;lastSpeedVoice=now;speak('Atención. Superas el límite de '+limit+' kilómetros por hora indicado en el mapa.');return;}const g=C.guidance(turns,progress,route.total,liveSpeed);currentInstruction=guidanceText(g);if(!g.turn){if(!voiceStages.has('final')){voiceStages.set('final',1);speak(currentInstruction);}return;}const rank={later:0,prepare:1,near:2,now:3}[g.stage],previous=voiceStages.get(g.index)||0;if(rank>previous){voiceStages.set(g.index,rank);speak(currentInstruction);} }
function paint(d){if(!completed)return;mideCapas();window.dispatchEvent(new CustomEvent("rutas:progress",{detail:{distance:d}}));completed.setLatLngs(C.section(route,0,d).map(ll));remaining.setLatLngs(C.section(route,d,Math.min(route.total,d+200)).map(ll));const g=C.guidance(turns,d,route.total,watch!==null?liveSpeed:0),end=d>=route.total-5,road=g.turn?.toRoad||(g.turn?.roadContext?'maniobra vial':'según el GPX');currentInstruction=end?'Final del recorrido.':guidanceText(g);$('nav-turn-icon').textContent=end?(falta()?'!':'✓'):g.turn?g.turn.symbol:'↑';$('nav-turn-text').textContent=end?'Final del recorrido':g.turn?(g.stage==='now'?'Ahora · ':'')+g.turn.label:'Sigue hasta el final';$('nav-turn-distance').textContent=end?(falta()||'Recorrido completado'):(g.stage==='now'?'En este punto':g.turn?'En '+metres(g.gap):metres(g.gap)+' restantes')+' · '+road;$('nav-after').textContent=g.next?'Después: '+g.next.label.toLowerCase()+' · '+metres(g.next.d-g.turn.d):'';hud(d,g,end);$('nav-live-summary').textContent=(watch!==null?'Navegación en directo':'Vista previa')+' · '+g.completed+' de '+turns.length+' indicaciones completadas';Array.from($('nav-turns').children).forEach((li,i)=>{li.classList.toggle('done',i<g.completed);li.classList.toggle('current',i===g.index);if(i===g.index)li.setAttribute('aria-current','step');else li.removeAttribute('aria-current');});}
$('drive-heading').onclick=function(){headingUp=!headingUp;const stage=$('nav-stage');
 stage.classList.toggle('heading-up',headingUp);
 this.setAttribute('aria-pressed',headingUp?'true':'false');
 this.textContent=headingUp?'▲':'N';
 controlLabel('drive-heading',headingUp?'Orientar el mapa al norte':'Orientar el mapa a la marcha');
 spin();
 if(map){map.invalidateSize();if(position&&follow)map.setView(position.getLatLng(),map.getZoom());}};
/* ---- compartir la ronda: la ruta viaja dentro del propio enlace ---- */
function b64url(bytes){let s='';for(const b of bytes)s+=String.fromCharCode(b);return btoa(s).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'');}
function unb64url(str){const pad=str.replace(/-/g,'+').replace(/_/g,'/');const bin=atob(pad+'==='.slice((pad.length+3)%4));const out=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)out[i]=bin.charCodeAt(i);return out;}
async function squeeze(text){if(typeof CompressionStream==='undefined')return null;
 try{const buf=await new Response(new Blob([text]).stream().pipeThrough(new CompressionStream('deflate-raw'))).arrayBuffer();return new Uint8Array(buf);}catch{return null;}}
async function unsqueeze(bytes){const buf=await new Response(new Blob([bytes]).stream().pipeThrough(new DecompressionStream('deflate-raw'))).arrayBuffer();return new TextDecoder().decode(buf);}
async function shareLink(pts,name){const packed=C.packRoute(name,pts),small=await squeeze(packed);
 const payload=small?'2'+b64url(small):'1'+b64url(new TextEncoder().encode(packed));
 return location.origin+location.pathname+'#r='+payload;}
function shareGPX(){const data=window.Roadbook.getRoute();if(!route||data.sample||access)return null;const name=RutasGPX.safeName(data.name),text=RutasGPX.build(name,data.pts,window.RutasMarks?window.RutasMarks.forExport():null),filename=RutasGPX.filename(name),blob=new Blob([text],{type:'application/gpx+xml;charset=utf-8'});return {name,text,filename,blob};}
function downloadGPX(file){const url=URL.createObjectURL(file.blob),a=document.createElement('a');a.href=url;a.download=file.filename;a.hidden=true;document.body.append(a);a.click();setTimeout(()=>{a.remove();URL.revokeObjectURL(url);},1500);}
async function nativeShareGPX(){const routeFile=shareGPX();$('share-box').hidden=false;$('share-link-box').hidden=true;$('share-qr').replaceChildren();if(!routeFile){$('share-note').textContent=access?'Termina primero el acceso al inicio para compartir tu recorrido.':'Carga primero tu GPX: el ejemplo no se comparte.';return false;}
 const file=typeof File==='function'?new File([routeFile.blob],routeFile.filename,{type:'application/gpx+xml',lastModified:Date.now()}):null;
 if(file&&navigator.share&&navigator.canShare?.({files:[file]})){try{await navigator.share({files:[file],title:routeFile.name,text:'Recorrido GPX: '+routeFile.name});$('share-note').textContent='Archivo '+routeFile.filename+' compartido correctamente.';return true;}catch(err){if(err.name==='AbortError'){$('share-note').textContent='Compartir cancelado. El archivo GPX sigue preparado.';return false;}$('share-note').textContent='No se pudo abrir el menú para compartir. Puedes guardar el GPX y enviarlo como archivo.';return false;}}
 $('share-note').textContent='Este navegador no permite enviar archivos directamente. Guarda el GPX y adjúntalo en WhatsApp, correo u otra aplicación.';return false;}
async function buildShare(){const data=window.Roadbook.getRoute();
 if(!route||data.sample||access){$('share-note').textContent=access?'Termina primero el acceso al inicio para compartir tu recorrido.':'Carga primero tu GPX: el ejemplo no se comparte.';$('share-box').hidden=false;$('share-url').value='';return;}
 const name=(data.name||'Ronda').replace(/\.gpx$/i,'');
 const url=await shareLink(data.pts,name);
 $('share-url').value=url;$('share-box').hidden=false;$('share-link-box').hidden=false;$('share-qr').replaceChildren();$('share-qr-btn').setAttribute('aria-expanded','false');$('share-qr-btn').textContent='Mostrar QR';
 $('share-note').textContent='Enlace web creado. Para conservar el archivo exacto y evitar enlaces cortados, usa preferentemente Compartir GPX.';
 return url;}
async function shareQR(){try{await window.RutasQR();}catch{$('share-note').textContent='El generador de QR no cargo. Usa Copiar enlace.';return;}
 const data=window.Roadbook.getRoute(),name=(data.name||'Ronda').replace(/\.gpx$/i,'');
 for(const eps of [0,2,3,5,10,20,40]){
  const pts=eps?C.simplify(data.pts,eps):data.pts;
  if(pts.length<2)continue;
  const url=await shareLink(pts,name);
  try{const qr=qrcode(0,'L');qr.addData(url);qr.make();
   $('share-qr').innerHTML=qr.createSvgTag({cellSize:4,margin:0,scalable:true});
   $('share-note').textContent=eps?('El QR no admite la ronda completa, asi que este codigo lleva una version aligerada: '+pts.length.toLocaleString('es-ES')+' de '+data.pts.length.toLocaleString('es-ES')+' puntos, con hasta '+eps+' m de desviacion. El enlace de arriba si lleva la ronda entera.')
    :('La ronda completa cabe en el QR: '+pts.length.toLocaleString('es-ES')+' puntos. Escanealo con la camara del movil.');
   return;}catch{}}
 $('share-qr').replaceChildren();
 $('share-note').textContent='Esta ronda no cabe en un codigo QR ni aligerada. Usa Copiar enlace.';}
$('nav-share').onclick=()=>{nativeShareGPX();};
$('share-file').onclick=()=>{nativeShareGPX();};
$('share-download').onclick=()=>{const file=shareGPX();if(!file){$('share-note').textContent='Carga primero un GPX válido.';return;}downloadGPX(file);$('share-note').textContent='Guardado '+file.filename+'. Ya puedes adjuntarlo como archivo.';};
$('share-link').onclick=()=>{buildShare();};
$('share-copy').onclick=async()=>{const url=$('share-url').value||await buildShare();if(!url)return;
 try{await navigator.clipboard.writeText(url);$('share-note').textContent='Enlace copiado. Quien lo abra vera la ronda cargada.';}
 catch{$('share-url').select();document.execCommand('copy');$('share-note').textContent='Enlace copiado.';}};
$('share-qr-btn').onclick=function(){if($('share-qr').firstChild){$('share-qr').replaceChildren();this.setAttribute('aria-expanded','false');this.textContent='Mostrar QR';return;}
 this.setAttribute('aria-expanded','true');this.textContent='Ocultar QR';shareQR();};
async function importFromHash(){const hash=location.hash||'';if(!hash.startsWith('#r='))return;
 const payload=hash.slice(3);history.replaceState(null,'',location.pathname);
 try{const bytes=unb64url(payload.slice(1));
  const text=payload[0]==='2'?await unsqueeze(bytes):new TextDecoder().decode(bytes);
  const {name,pts}=C.unpackRoute(text);
  window.Roadbook.restoreRoute([{name,pts}],name+'.gpx');
  message('Ronda recibida por enlace: '+name+' ('+pts.length.toLocaleString('es-ES')+' puntos). Queda guardada en este dispositivo.');
 }catch(err){message('No se pudo abrir la ruta del enlace. '+err.message,true);}}
importFromHash();
$('nav-repeat').onclick=()=>{if(!$('nav-voice').checked)$('nav-voice').checked=true;speak(window.RutasChecks?.gpsAlert(explore)||currentInstruction,true);};
$('nav-voice').onchange=()=>{if(!$('nav-voice').checked&&window.speechSynthesis)window.speechSynthesis.cancel();else if(watch!==null)speak(currentInstruction,true);};
function drawArrows(){if(arrowLayer)layer.removeLayer(arrowLayer);arrowLayer=L.featureGroup().addTo(layer);const scale=156543.03*Math.cos(route.pts[0].lat*Math.PI/180)/Math.pow(2,map.getZoom()),step=Math.max(60,scale*70,route.total/200),seen=new Set();for(let d=step;d<route.total;d+=step){const p=C.at(route,d),pixel=map.latLngToLayerPoint(ll(p)),key=Math.round(pixel.x/40)+':'+Math.round(pixel.y/40);if(seen.has(key))continue;seen.add(key);const h=C.heading(C.at(route,d-15),C.at(route,Math.min(route.total,d+15)));L.marker(ll(p),{interactive:false,icon:L.divIcon({className:'route-arrow',html:'<span style="transform:rotate('+(h-90)+'deg)">➤</span>',iconSize:[16,16],iconAnchor:[8,8]})}).addTo(arrowLayer);}}

function drawTurns(){const list=$('nav-turns');list.replaceChildren();$('nav-turn-count').textContent='('+turns.length+')';for(const t of turns){const li=document.createElement('li'),b=document.createElement('button'),source=t.toRoad?t.toRoad:(t.roadContext?'maniobra vial':'estimado GPX');b.type='button';b.textContent=metres(t.d)+' · '+t.symbol+' '+t.label+' · '+source;b.onclick=()=>{if(watch!==null){message('Para el GPS antes de cambiar el punto del recorrido.',true);return;}pause();showPreview(Math.max(0,t.d-25),true);map.setZoom(18);};li.append(b);list.append(li);}}
function saveLocal(data){if(access||data.sample)return;try{localStorage.setItem('rutas-route',JSON.stringify({name:data.name,tracks:data.tracks,selected:data.selected}));$('nav-local').textContent='Ruta guardada solo en este navegador. No se publica ni se sube a un servidor.';}catch{$('nav-local').textContent='No se pudo guardar la ruta en este navegador. Puedes usarla, pero tendrás que cargarla de nuevo al volver.';}}
function restoreLocal(){try{const data=JSON.parse(localStorage.getItem('rutas-route'));if(!data||!Array.isArray(data.tracks)||!data.tracks.length)return;const valid=data.tracks.every(t=>Array.isArray(t.pts)&&t.pts.length>1&&t.pts.every(p=>Number.isFinite(p.lat)&&Number.isFinite(p.lon)&&Math.abs(p.lat)<=90&&Math.abs(p.lon)<=180));if(!valid)return;window.Roadbook.restoreRoute(data.tracks,String(data.name||'Ruta guardada'));if(Number.isInteger(data.selected)&&data.selected>0&&data.selected<data.tracks.length)window.Roadbook.selectTrack(data.selected);}catch{}}


function restoreProgress(){resumePoint=null;$('nav-resume').hidden=true;$('nav-passes').replaceChildren();$('resume-status').textContent='El avance se guarda solo en este dispositivo.';try{const saved=JSON.parse(localStorage.getItem(resumeKey));if(saved&&Number.isFinite(saved.d)&&saved.d>0&&saved.d<route.total-15){resumePoint=saved.d;$('nav-resume').hidden=false;$('nav-resume').textContent='Continuar desde '+metres(saved.d);$('resume-status').textContent='Avance guardado: '+metres(saved.d)+' de '+metres(route.total)+'. Elige continuar o empieza desde el inicio.';}}catch{}}
function saveProgress(force=false){if(access||!resumeKey||!route||Roadbook.getRoute().sample||(!force&&Date.now()-lastSaved<5000))return;lastSaved=Date.now();try{localStorage.setItem(resumeKey,JSON.stringify({d:progress,saved:new Date().toISOString()}));}catch{$('resume-status').textContent='No se pudo guardar el avance. El almacenamiento del navegador está lleno o no disponible.';}}
$('nav-resume').onclick=()=>{if(watch!==null||resumePoint===null)return;pause();showPreview(resumePoint,true);map.setZoom(18);message('Punto guardado seleccionado. Pulsa Iniciar navegación para continuar.');};
$('nav-locate-pass').onclick=()=>{if(watch!==null)return;if(!navigator.geolocation){message('Este navegador no permite acceder al GPS.',true);return;}const selected=route;$('nav-locate-pass').disabled=true;$('resume-status').textContent='Buscando tu posición para localizar las pasadas cercanas…';navigator.geolocation.getCurrentPosition(pos=>{if(route!==selected)return;$('nav-locate-pass').disabled=false;$('nav-passes').replaceChildren();if(pos.coords.accuracy>40){$('resume-status').textContent='La precisión GPS es insuficiente para distinguir calles. Inténtalo de nuevo.';return;}const candidates=C.nearbyPasses(route,{lat:pos.coords.latitude,lon:pos.coords.longitude});$('resume-status').textContent=candidates.length?'Elige por qué pasada vas. No cambiaremos el recorrido automáticamente.':'No encuentro una pasada a menos de 60 m. Selecciona el inicio con el control de distancia.';for(const c of candidates){const li=document.createElement('li'),b=document.createElement('button');b.type='button';b.className='btn sm';b.textContent='Pasada en '+metres(c.d)+' · a '+Math.round(c.error)+' m';b.onclick=()=>{if(watch!==null)return;pause();showPreview(c.d,true);map.setZoom(18);message('Pasada seleccionada. Pulsa Iniciar navegación.');};li.append(b);$('nav-passes').append(li);}},()=>{if(route!==selected)return;$('nav-locate-pass').disabled=false;$('resume-status').textContent='No se pudo localizar tu posición. Revisa el permiso de ubicación.';},{enableHighAccuracy:true,maximumAge:0,timeout:15000});};
window.addEventListener('rutas:roads',e=>{if(access||!route||e.detail.pts!==route.pts)return;turns=RutasRestrictions.enrichTurns(route,baseTurns,e.detail.elements);voiceStages.clear();drawTurns();paint(explore);});

function focusMap(active){$('nav-stage').classList.toggle('focus',active);document.body.classList.toggle('map-focus',active);$('nav-focus').textContent=active?'Reducir mapa':'Ampliar mapa';$('nav-focus').setAttribute('aria-pressed',String(active));setTimeout(()=>map&&map.invalidateSize(),50);}
function setDriving(active){if(!active&&headingUp){headingUp=false;$('nav-stage').classList.remove('heading-up');$('drive-heading').setAttribute('aria-pressed','false');$('drive-heading').textContent='N';spin();}$('nav-stage').classList.toggle('driving',active);$('drive-tools').hidden=!active;$('drive-panel').hidden=!active;if(!active)$('nav-road-alert').hidden=true;focusMap(active);if(active){const settings=document.querySelector('.route-settings');if(settings)settings.open=false;roadVoiceStages.clear();$('drive-speed').textContent='—';$('drive-voice').textContent=$('nav-voice').checked?'Voz activada':'Voz desactivada';$('drive-voice').setAttribute('aria-pressed',String($('nav-voice').checked));$('drive-center').classList.remove('needs-center');$('drive-center').textContent='Centrar GPS';if(route){metrics(explore);map.setView(ll(C.at(route,explore)),18,{animate:false});}paintBanner(H.banner({paused:'Buscando tu posición',arrow:'⌖'}));$('drive-next').textContent='';$('drive-eta-time').textContent='—';$('drive-eta-clock').textContent='—';$('nav-turn-icon').textContent='⌖';$('nav-turn-text').textContent='Buscando tu posición';$('nav-turn-distance').textContent='Permite la ubicación para comenzar';$('nav-after').textContent='';}}
function followCamera(p){const z=liveSpeed>20?16:liveSpeed>8?17:18;const point=map.project(ll(p),z);point.y-=map.getSize().y*.08;map.setView(map.unproject(point,z),z,{animate:false});}
$('drive-exit').onclick=()=>stop();
$('drive-center').onclick=()=>{follow=true;$('drive-center').classList.remove('needs-center');$('drive-center').textContent='Centrar GPS';controlLabel('drive-center','Centrar mi posición');if(position){const p=position.getLatLng();followCamera({lat:p.lat,lon:p.lng});}else message('Buscando tu posición GPS…');};
$('drive-voice').onclick=()=>{$('nav-voice').checked=!$('nav-voice').checked;$('nav-voice').dispatchEvent(new Event('change'));const voiceOn=$('nav-voice').checked;$('drive-voice').textContent=voiceOn?'Voz activada':'Voz desactivada';$('drive-voice').setAttribute('aria-pressed',String(voiceOn));controlLabel('drive-voice',voiceOn?'Desactivar indicaciones por voz':'Activar indicaciones por voz');};

$('nav-focus').onclick=()=>focusMap(!$('nav-stage').classList.contains('focus'));
document.addEventListener('keydown',e=>{if(e.key==='Escape'&&$('nav-stage').classList.contains('focus'))$('nav-focus').click();});
$('nav-reset').onclick=()=>{pause();showPreview(0,true);map.setZoom(17);};
$('nav-forget').onclick=()=>{try{localStorage.removeItem('rutas-route');if(resumeKey)localStorage.removeItem(resumeKey);resumePoint=null;$('nav-resume').hidden=true;$('nav-local').textContent='Ruta guardada borrada. La ruta actual sigue disponible hasta que cierres la página.';}catch{$('nav-local').textContent='No se pudo acceder al almacenamiento del navegador.';}};

$('nav-stop').onclick=()=>stop();$('nav-focus-stop').onclick=()=>stop();$('nav-fit').onclick=()=>{follow=false;map.fitBounds(layer.getBounds(),{padding:[25,25]});};$('nav-center').onclick=()=>{follow=true;if(position)map.setView(position.getLatLng(),17);else message('Pulsa Iniciar GPS para mostrar tu posición.');};$('nav-progress').oninput=e=>showPreview(route.total*Number(e.target.value)/1000,true);$('nav-next').onclick=()=>showPreview(explore+50,true);$('nav-back').onclick=()=>showPreview(explore-50,true);$('nav-play').onclick=()=>{if(playing){pause();return;}if(explore>=route.total)showPreview(0);$('nav-play').textContent='Pausar';focusMap(true);   // se pulsa reproducir para mirar: el mapa se abre entero
 playing=setInterval(()=>{showPreview(explore+Number($("nav-play-speed").value),true);if(explore>=route.total)pause();},500);};$('nav-file').onchange=e=>{window.Roadbook.readFile(e.target.files[0]);e.target.value='';};$('nav-track').onchange=e=>window.Roadbook.selectTrack(Number(e.target.value));window.addEventListener('rutas:route',()=>{access=null;sync();});window.addEventListener('rutas:visible',()=>{sync();setTimeout(()=>map&&map.invalidateSize(),50);});document.addEventListener('visibilitychange',()=>{if(document.hidden){pause();}else if(watch!==null)acquire(session);});window.addEventListener('pagehide',()=>stop(true));window.RutasMap={beginAccess:data=>{if(watch!==null||access)throw Error('Detén la navegación antes de calcular un acceso.');const targetD=explore,target=C.at(route,targetD);progress=explore;saveProgress(true);access={...data,targetD,target};routeRef=null;sync();$('nav-start').click();},beginRecovery:(data,targetD,target)=>{if(access)throw Error('Ya hay un acceso activo.');if(watch!==null)stop(true,true);progress=explore;saveProgress(true);access={...data,targetD,target,recovery:true};routeRef=null;sync();$('nav-start').click();},stopAccess:()=>{if(access)stop(true);},getAccess:()=>access?{targetD:access.targetD,target:access.target,recovery:!!access.recovery}:null,refresh:()=>paint(explore),get:()=>({map,route,mode:access?'access':'gpx',progress:explore,active:watch!==null,follow,headingUp,bearing:lastBearing,speed:liveSpeed,satellite,location:watch!==null&&position?position.getLatLng():null}),setSatellite,setFollow:value=>{follow=value;$('drive-center').classList.toggle('needs-center',!value);$('drive-center').textContent=value?'Centrar GPS':'Volver a seguir';},seek:d=>{if(watch!==null){message("Para la navegación para explorar este aviso.",true);return;}pause();showPreview(d,true);map.setZoom(18);}};restoreLocal();sync();
window.RutasMap.previewPosition=previewPosition;
window.RutasMap.useStreetPath=useStreetPath;
})();
